# GP-200 Web Editor Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Browser-basierter Next.js-Editor zum Dekodieren, Bearbeiten und Re-Encodieren von Valeton GP-200 `.prst` Preset-Dateien — vollständig client-seitig, kein Server, kein Gerät nötig.

**Architecture:** Drei Schichten — eine pure-TypeScript Binary Engine (`/src/core`) liest/schreibt `.prst`-Buffer; eine React-UI-Schicht (`/src/components`) mit vollständiger ARIA-Unterstützung; ein `usePreset`-Hook verbindet beide. Alle sichtbaren Strings sind i18n-gepeilt (DE/EN via next-intl). State läuft komplett client-seitig via File API.

**Tech Stack:** Next.js 14 (App Router), TypeScript strict, Tailwind CSS, Radix UI/Headless, next-intl 3, Zod, Vitest, Playwright + @axe-core/playwright

> ⚠️ **Hinweis Binary-Format:** Das genaue Byte-Layout der `.prst`-Datei muss noch reverse-engineered werden (Sample-Dateien vom Gerät nötig). Die Engine wird spec-driven designed — Byte-Offsets sind zunächst als Platzhalter markiert.

---

## Dateistruktur

```
gp200editor/
├── src/
│   ├── app/
│   │   ├── [locale]/
│   │   │   ├── layout.tsx        # Root Layout mit NextIntlClientProvider
│   │   │   ├── page.tsx          # Startseite (Upload-CTA)
│   │   │   └── editor/
│   │   │       └── page.tsx      # Editor-Seite (nach Upload)
│   │   └── favicon.ico
│   ├── components/
│   │   ├── Navbar.tsx            # Navigation mit Sprachschalter
│   │   ├── Footer.tsx            # Footer
│   │   ├── FileUpload.tsx        # Drag-&-Drop .prst Upload
│   │   └── EffectSlot.tsx        # Ein Effekt-Block im Editor
│   ├── core/
│   │   ├── types.ts              # GP200Preset, EffectSlot, Zod-Schemas
│   │   ├── BinaryParser.ts       # Low-level Buffer-Lese-Helfer
│   │   ├── BufferGenerator.ts    # Low-level Buffer-Schreib-Helfer
│   │   ├── PRSTDecoder.ts        # .prst → GP200Preset
│   │   └── PRSTEncoder.ts        # GP200Preset → .prst ArrayBuffer
│   │   # (constants.ts wird hinzugefügt wenn echte Byte-Offsets bekannt sind)
│   ├── hooks/
│   │   └── usePreset.ts          # State: aktuelles Preset, Dispatch-Actions
│   └── i18n/
│       ├── routing.ts            # defineRouting (locales: ['de','en'])
│       └── request.ts            # getRequestConfig
├── src/middleware.ts             # next-intl createMiddleware
├── messages/
│   ├── de.json
│   └── en.json
├── tests/
│   ├── unit/
│   │   ├── smoke.test.ts
│   │   ├── types.test.ts
│   │   ├── BinaryParser.test.ts
│   │   ├── BufferGenerator.test.ts
│   │   ├── PRSTDecoder.test.ts
│   │   ├── PRSTEncoder.test.ts
│   │   └── usePreset.test.ts
│   └── e2e/
│       ├── editor.spec.ts
│       └── a11y.spec.ts
├── docs/superpowers/plans/
├── planung/                      # (existiert bereits)
├── next.config.ts
├── tailwind.config.ts
├── vitest.config.ts
└── playwright.config.ts
```

---

## Chunk 1: Foundation

### Task 1: Next.js Projekt aufsetzen

**Files:**
- Create: `package.json`, `next.config.ts`, `tsconfig.json`, `tailwind.config.ts` (via create-next-app)

- [ ] **Step 1: Projekt initialisieren**

```bash
cd /home/manuel/claude/gp200editor
npx create-next-app@14 . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --no-turbopack
```

Bei Prompts: alle Defaults bestätigen (Enter).

- [ ] **Step 2: Abhängigkeiten installieren**

```bash
npm install next-intl zod @radix-ui/react-slot
npm install -D vitest @vitejs/plugin-react @testing-library/react \
  @testing-library/jest-dom jsdom @vitest/coverage-v8 \
  playwright @playwright/test @axe-core/playwright
```

- [ ] **Step 3: TypeScript strict prüfen**

In `tsconfig.json` sicherstellen:
```json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true
  }
}
```

- [ ] **Step 4: Commit**

```bash
git init
git add .
git commit -m "chore: initial Next.js 14 scaffold with TypeScript strict + Tailwind"
```

---

### Task 2: Vitest konfigurieren

**Files:**
- Create: `vitest.config.ts`
- Create: `tests/unit/.gitkeep`

- [ ] **Step 1: Failing-Test schreiben (Smoke-Test)**

Erstelle `tests/unit/smoke.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';

describe('Vitest', () => {
  it('is configured correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 2: Vitest-Konfiguration erstellen**

Erstelle `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
    include: ['tests/unit/**/*.test.ts', 'tests/unit/**/*.test.tsx'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

- [ ] **Step 3: package.json Scripts ergänzen**

In `package.json` unter `"scripts"` hinzufügen:
```json
"test": "vitest run",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage"
```

- [ ] **Step 4: Test ausführen — muss PASS sein**

```bash
npx vitest run tests/unit/smoke.test.ts
```
Erwartet: `✓ tests/unit/smoke.test.ts (1)`

- [ ] **Step 5: Commit**

```bash
git add vitest.config.ts tests/unit/smoke.test.ts package.json
git commit -m "chore: configure Vitest with jsdom environment"
```

---

### Task 3: Playwright konfigurieren

**Files:**
- Create: `playwright.config.ts`
- Create: `tests/e2e/.gitkeep`

- [ ] **Step 1: Browser installieren**

```bash
npx playwright install chromium
```

- [ ] **Step 2: playwright.config.ts erstellen**

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

- [ ] **Step 3: package.json Script ergänzen**

```json
"test:e2e": "playwright test"
```

- [ ] **Step 4: .gitkeep anlegen und Konfiguration prüfen**

```bash
touch tests/e2e/.gitkeep
npx playwright test --list
```
Erwartet: Keine Fehler beim Parsen der Konfiguration (auch wenn 0 Tests gefunden werden).

- [ ] **Step 5: Commit**

```bash
git add playwright.config.ts package.json tests/e2e/.gitkeep
git commit -m "chore: configure Playwright with Chromium"
```

---

### Task 4: next-intl i18n Setup

**Files:**
- Create: `src/i18n/routing.ts`
- Create: `src/i18n/request.ts`
- Create: `src/middleware.ts`
- Create: `messages/de.json`
- Create: `messages/en.json`
- Modify: `next.config.ts`
- Move/Modify: `src/app/globals.css` → bleibt an Ort, Import-Pfad in layout.tsx wird auf `'../globals.css'` gesetzt

- [ ] **Step 1: Routing definieren**

Erstelle `src/i18n/routing.ts`:
```typescript
import { defineRouting } from 'next-intl/routing';
import { createNavigation } from 'next-intl/navigation';

export const routing = defineRouting({
  locales: ['de', 'en'],
  defaultLocale: 'de',
});

// Typisierte Navigation-Helfer — importiere Link, useRouter, usePathname aus diesem Modul
// statt aus 'next/navigation', um locale-aware Routing zu erhalten
export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
```

- [ ] **Step 2: Request-Konfiguration**

Erstelle `src/i18n/request.ts`:
```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;
  if (!locale || !routing.locales.includes(locale as 'de' | 'en')) {
    locale = routing.defaultLocale;
  }
  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 3: Middleware erstellen**

Erstelle `src/middleware.ts`:
```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: ['/((?!_next|_vercel|.*\\..*).*)'],
};
```

- [ ] **Step 4: next.config.ts anpassen**

```typescript
import createNextIntlPlugin from 'next-intl/plugin';
import type { NextConfig } from 'next';

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

const nextConfig: NextConfig = {};

export default withNextIntl(nextConfig);
```

- [ ] **Step 5: Basis-Nachrichten anlegen**

Erstelle `messages/de.json`:
```json
{
  "nav": {
    "title": "GP-200 Editor",
    "home": "Start",
    "editor": "Editor"
  },
  "home": {
    "headline": "Valeton GP-200 Preset Editor",
    "subtitle": "Lade deine .prst-Datei, bearbeite Effekte und speichere zurück.",
    "uploadCta": "Preset laden"
  },
  "footer": {
    "disclaimer": "Inoffizielles Tool. Nicht von Valeton."
  },
  "editor": {
    "title": "Preset bearbeiten",
    "patchName": "Patch-Name",
    "download": "Preset herunterladen"
  }
}
```

Erstelle `messages/en.json`:
```json
{
  "nav": {
    "title": "GP-200 Editor",
    "home": "Home",
    "editor": "Editor"
  },
  "home": {
    "headline": "Valeton GP-200 Preset Editor",
    "subtitle": "Load your .prst file, edit effects and save it back.",
    "uploadCta": "Load preset"
  },
  "footer": {
    "disclaimer": "Unofficial tool. Not affiliated with Valeton."
  },
  "editor": {
    "title": "Edit Preset",
    "patchName": "Patch Name",
    "download": "Download preset"
  }
}
```

- [ ] **Step 6: App-Verzeichnis auf [locale] umstrukturieren**

```bash
mkdir -p src/app/[locale]
# Bestehende page.tsx + layout.tsx in src/app/[locale]/ verschieben
# WICHTIG: globals.css bleibt in src/app/ — Import-Pfad im neuen layout.tsx: '../globals.css'
mv src/app/page.tsx src/app/[locale]/page.tsx
mv src/app/layout.tsx src/app/[locale]/layout.tsx
```

- [ ] **Step 7: layout.tsx für next-intl anpassen**

Erstelle `src/app/[locale]/layout.tsx`:
```typescript
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { routing } from '@/i18n/routing';
import { notFound } from 'next/navigation';
import type { Metadata } from 'next';
import '../globals.css'; // globals.css bleibt in src/app/, daher relativer Pfad nach oben

export const metadata: Metadata = {
  title: 'GP-200 Editor',
  description: 'Unofficial Valeton GP-200 Preset Editor',
};

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = await params;
  if (!routing.locales.includes(locale as 'de' | 'en')) notFound();
  const messages = await getMessages();
  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 8: Build prüfen (vor dem Commit)**

```bash
npm run build
```
Erwartet: Build erfolgreich, keine TypeScript-Fehler, keine fehlenden Module.

- [ ] **Step 9: Commit**

```bash
git add src/i18n/ src/middleware.ts messages/ src/app/ next.config.ts
git commit -m "feat: add next-intl i18n with DE/EN support"
```

---

### Task 5: Navbar-Komponente

**Files:**
- Create: `src/components/Navbar.tsx`
- Modify: `src/app/[locale]/layout.tsx`
- Test: keine Unit-Tests (visuelle Komponente, E2E in Chunk 3)

- [ ] **Step 1: Navbar schreiben**

Erstelle `src/components/Navbar.tsx`:
```typescript
'use client';
import { useTranslations, useLocale } from 'next-intl';
import { Link, useRouter, usePathname } from '@/i18n/routing'; // next-intl typed navigation

export function Navbar() {
  const t = useTranslations('nav');
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const otherLocale = locale === 'de' ? 'en' : 'de';

  function switchLocale() {
    // next-intl router.replace mit locale-Option — kein fragiles string.replace
    router.replace(pathname, { locale: otherLocale });
  }

  return (
    <nav
      role="navigation"
      aria-label={t('title')}
      className="flex items-center justify-between px-6 py-4 bg-gray-900 text-white"
    >
      <Link
        href="/"
        className="text-xl font-bold"
        data-testid="nav-home-link"
      >
        {t('title')}
      </Link>
      <div className="flex gap-4 items-center">
        <Link
          href="/"
          className="hover:underline"
          data-testid="nav-link-home"
        >
          {t('home')}
        </Link>
        <Link
          href="/editor"
          className="hover:underline"
          data-testid="nav-link-editor"
        >
          {t('editor')}
        </Link>
        <button
          onClick={switchLocale}
          aria-label={`Switch to ${otherLocale.toUpperCase()}`}
          data-testid="nav-locale-switcher"
          className="px-3 py-1 border border-white rounded hover:bg-white hover:text-gray-900 transition"
        >
          {otherLocale.toUpperCase()}
        </button>
      </div>
    </nav>
  );
}
```

> ⚠️ Voraussetzung: `src/i18n/routing.ts` muss neben `routing` auch die typisierten Navigations-Helfer exportieren. Ergänze in `src/i18n/routing.ts`:
> ```typescript
> export const { Link, redirect, usePathname, useRouter } =
>   createNavigation(routing);
> ```
> Füge `import { createNavigation } from 'next-intl/navigation';` hinzu.

- [ ] **Step 2: Navbar in Layout einbauen**

In `src/app/[locale]/layout.tsx` innerhalb von `<body>`:
```typescript
import { Navbar } from '@/components/Navbar';
// ...
<body>
  <NextIntlClientProvider messages={messages}>
    <Navbar />
    <main>{children}</main>
  </NextIntlClientProvider>
</body>
```

- [ ] **Step 3: Visuell prüfen**

```bash
npm run dev
```
Navbar auf http://localhost:3000/de sichtbar, Sprachschalter wechselt zu /en.

- [ ] **Step 4: Commit**

```bash
git add src/components/Navbar.tsx src/app/[locale]/layout.tsx
git commit -m "feat: add accessible Navbar with locale switcher"
```

---

### Task 6: Footer-Komponente

**Files:**
- Create: `src/components/Footer.tsx`
- Modify: `src/app/[locale]/layout.tsx`

- [ ] **Step 1: Footer schreiben**

Erstelle `src/components/Footer.tsx`:
```typescript
import { useTranslations } from 'next-intl';

export function Footer() {
  const t = useTranslations('footer');
  return (
    <footer
      role="contentinfo"
      className="px-6 py-4 bg-gray-900 text-gray-400 text-sm text-center"
      data-testid="footer"
    >
      {t('disclaimer')}
    </footer>
  );
}
```

- [ ] **Step 2: Footer in Layout einbauen**

```typescript
import { Footer } from '@/components/Footer';
// In <body>:
<body className="flex flex-col min-h-screen">
  <NextIntlClientProvider messages={messages}>
    <Navbar />
    <main className="flex-1">{children}</main>
    <Footer />
  </NextIntlClientProvider>
</body>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/Footer.tsx src/app/[locale]/layout.tsx
git commit -m "feat: add Footer with disclaimer"
```

---

### Task 7: Startseite

**Files:**
- Modify: `src/app/[locale]/page.tsx`

- [ ] **Step 1: Startseite schreiben**

Erstelle `src/app/[locale]/page.tsx`:
```typescript
import { getTranslations, getLocale } from 'next-intl/server'; // async Server Component → getTranslations statt useTranslations
import { Link } from '@/i18n/routing';

export default async function HomePage() {
  const t = await getTranslations('home');
  const locale = await getLocale();
  return (
    <div className="flex flex-col items-center justify-center flex-1 px-4 py-20 text-center">
      <h1 className="text-4xl font-bold mb-4">{t('headline')}</h1>
      <p className="text-lg text-gray-600 mb-8 max-w-md">{t('subtitle')}</p>
      <Link
        href={`/${locale}/editor`}
        className="px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition"
        data-testid="home-upload-cta"
      >
        {t('uploadCta')}
      </Link>
    </div>
  );
}
```

- [ ] **Step 2: Editor-Seite (Platzhalter) erstellen**

Erstelle `src/app/[locale]/editor/page.tsx`:
```typescript
import { getTranslations } from 'next-intl/server';

export default async function EditorPage() {
  const t = await getTranslations('editor');
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">{t('title')}</h1>
      <p className="text-gray-500 mt-2">{t('placeholder')}</p>
    </div>
  );
}
```

Ergänze `"placeholder"` in beiden Message-Dateien:
- `messages/de.json` unter `"editor"`: `"placeholder": "Editor wird in Chunk 3 implementiert."`
- `messages/en.json` unter `"editor"`: `"placeholder": "Editor will be implemented in Chunk 3."`

- [ ] **Step 3: Build prüfen**

```bash
npm run build
```
Erwartet: Build erfolgreich, keine TypeScript-Fehler.

- [ ] **Step 4: Commit**

```bash
git add src/app/
git commit -m "feat: add home page and editor placeholder"
```

---

## Chunk 2: Die Engine

> Die Binary Engine ist komplett unabhängig von Next.js — pure TypeScript, vollständig testbar ohne Browser.

### Task 8: Typen & Zod-Schemas

**Files:**
- Create: `src/core/types.ts`
- Test: `tests/unit/types.test.ts`

- [ ] **Step 1: Failing-Test schreiben**

Erstelle `tests/unit/types.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { GP200PresetSchema, EffectSlotSchema } from '@/core/types';

describe('GP200PresetSchema', () => {
  it('validates a valid preset', () => {
    const result = GP200PresetSchema.safeParse({
      version: '1',   // Konsistent mit Decoder-Ausgabe: String(readUint8())
      patchName: 'TestPatch',
      effects: [],
      checksum: 0xab,
    });
    expect(result.success).toBe(true);
  });

  it('rejects patchName longer than 12 chars', () => {
    const result = GP200PresetSchema.safeParse({
      version: '1',
      patchName: 'ThisNameIsTooLong',
      effects: [],
      checksum: 0,
    });
    expect(result.success).toBe(false);
  });
});

describe('EffectSlotSchema', () => {
  it('validates a valid effect slot', () => {
    const result = EffectSlotSchema.safeParse({
      slotIndex: 0,
      effectId: 5,
      enabled: true,
      params: [127, 64, 0],
    });
    expect(result.success).toBe(true);
  });

  it('rejects slotIndex out of range (max 9)', () => {
    const result = EffectSlotSchema.safeParse({
      slotIndex: 10,
      effectId: 0,
      enabled: false,
      params: [],
    });
    expect(result.success).toBe(false);
  });

  it('rejects param value out of byte range (max 255)', () => {
    const result = EffectSlotSchema.safeParse({
      slotIndex: 0,
      effectId: 0,
      enabled: true,
      params: [256],
    });
    expect(result.success).toBe(false);
  });
});
```

- [ ] **Step 2: Test ausführen — muss FAIL sein**

```bash
npx vitest run tests/unit/types.test.ts
```
Erwartet: FAIL (Modul nicht gefunden)

- [ ] **Step 3: types.ts implementieren**

Erstelle `src/core/types.ts`:
```typescript
import { z } from 'zod';

export const EffectSlotSchema = z.object({
  slotIndex: z.number().int().min(0).max(9),
  effectId: z.number().int().min(0),
  enabled: z.boolean(),
  /** Parameter-Array — Länge und Bedeutung sind effektabhängig */
  params: z.array(z.number().int().min(0).max(255)),
});

export const GP200PresetSchema = z.object({
  version: z.string(),
  patchName: z.string().max(12),
  effects: z.array(EffectSlotSchema),
  checksum: z.number().int().min(0).max(255),
});

export type EffectSlot = z.infer<typeof EffectSlotSchema>;
export type GP200Preset = z.infer<typeof GP200PresetSchema>;
```

- [ ] **Step 4: Tests ausführen — müssen PASS sein**

```bash
npx vitest run tests/unit/types.test.ts
```
Erwartet: 5 Tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/core/types.ts tests/unit/types.test.ts
git commit -m "feat(core): add GP200Preset and EffectSlot Zod schemas"
```

---

### Task 9: BinaryParser

**Files:**
- Create: `src/core/BinaryParser.ts`
- Test: `tests/unit/BinaryParser.test.ts`

- [ ] **Step 1: Failing-Tests schreiben**

Erstelle `tests/unit/BinaryParser.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { BinaryParser } from '@/core/BinaryParser';

describe('BinaryParser', () => {
  const buf = new Uint8Array([0x47, 0x50, 0x00, 0x0a, 0x41, 0x42]);

  it('reads a single byte at offset', () => {
    const p = new BinaryParser(buf);
    expect(p.readUint8(0)).toBe(0x47);
    expect(p.readUint8(1)).toBe(0x50);
  });

  it('reads a fixed-length ASCII string', () => {
    const p = new BinaryParser(buf);
    expect(p.readAscii(0, 2)).toBe('GP');
  });

  it('reads a uint16 little-endian', () => {
    // bytes [0x47, 0x50] → 0x5047
    const p = new BinaryParser(buf);
    expect(p.readUint16LE(0)).toBe(0x5047);
  });

  it('throws RangeError on out-of-bounds uint8 read', () => {
    const p = new BinaryParser(buf);
    expect(() => p.readUint8(100)).toThrow(RangeError);
  });

  it('throws RangeError when readUint16LE spans past end of buffer', () => {
    const p = new BinaryParser(buf); // length 6
    expect(() => p.readUint16LE(5)).toThrow(RangeError); // needs 2 bytes, only 1 left
  });

  it('throws RangeError when readAscii spans past end of buffer', () => {
    const p = new BinaryParser(buf); // length 6
    expect(() => p.readAscii(4, 10)).toThrow(RangeError);
  });
});
```

- [ ] **Step 2: Test ausführen — muss FAIL sein**

```bash
npx vitest run tests/unit/BinaryParser.test.ts
```

- [ ] **Step 3: BinaryParser implementieren**

Erstelle `src/core/BinaryParser.ts`:
```typescript
export class BinaryParser {
  private view: DataView;

  constructor(buffer: Uint8Array) {
    this.view = new DataView(buffer.buffer, buffer.byteOffset, buffer.byteLength);
  }

  private assertBounds(offset: number, size: number): void {
    if (offset + size > this.view.byteLength) {
      throw new RangeError(
        `BinaryParser: out of bounds read at offset ${offset} (size ${size}, buffer ${this.view.byteLength})`
      );
    }
  }

  readUint8(offset: number): number {
    this.assertBounds(offset, 1);
    return this.view.getUint8(offset);
  }

  readUint16LE(offset: number): number {
    this.assertBounds(offset, 2);
    return this.view.getUint16(offset, true);
  }

  readUint32LE(offset: number): number {
    this.assertBounds(offset, 4);
    return this.view.getUint32(offset, true);
  }

  readAscii(offset: number, length: number): string {
    this.assertBounds(offset, length);
    let result = '';
    for (let i = 0; i < length; i++) {
      const byte = this.view.getUint8(offset + i);
      if (byte === 0) break; // null-terminated
      result += String.fromCharCode(byte);
    }
    return result;
  }

  get byteLength(): number {
    return this.view.byteLength;
  }
}
```

- [ ] **Step 4: Tests ausführen — müssen PASS sein**

```bash
npx vitest run tests/unit/BinaryParser.test.ts
```
Erwartet: 6 Tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/core/BinaryParser.ts tests/unit/BinaryParser.test.ts
git commit -m "feat(core): add BinaryParser with bounds checking"
```

---

### Task 10: BufferGenerator

**Files:**
- Create: `src/core/BufferGenerator.ts`
- Test: `tests/unit/BufferGenerator.test.ts`

- [ ] **Step 1: Failing-Tests schreiben**

Erstelle `tests/unit/BufferGenerator.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { BufferGenerator } from '@/core/BufferGenerator';

describe('BufferGenerator', () => {
  it('writes a byte and returns correct buffer', () => {
    const gen = new BufferGenerator(4);
    gen.writeUint8(0, 0xab);
    expect(gen.toUint8Array()[0]).toBe(0xab);
  });

  it('writes an ASCII string (null-padded)', () => {
    const gen = new BufferGenerator(6);
    gen.writeAscii(0, 'GP', 4);
    const arr = gen.toUint8Array();
    expect(arr[0]).toBe(0x47); // 'G'
    expect(arr[1]).toBe(0x50); // 'P'
    expect(arr[2]).toBe(0x00); // null-pad
    expect(arr[3]).toBe(0x00); // null-pad
  });

  it('throws RangeError on out-of-bounds write', () => {
    const gen = new BufferGenerator(2);
    expect(() => gen.writeUint8(5, 0xff)).toThrow(RangeError);
  });

  it('writes a uint16 little-endian correctly', () => {
    const gen = new BufferGenerator(4);
    gen.writeUint16LE(0, 0x1234);
    const arr = gen.toUint8Array();
    expect(arr[0]).toBe(0x34); // low byte first
    expect(arr[1]).toBe(0x12); // high byte second
  });

  it('throws RangeError when writeUint16LE spans past end', () => {
    const gen = new BufferGenerator(3);
    expect(() => gen.writeUint16LE(2, 0x00)).toThrow(RangeError); // only 1 byte left
  });
});
```

- [ ] **Step 2: Test ausführen — muss FAIL sein**

```bash
npx vitest run tests/unit/BufferGenerator.test.ts
```

- [ ] **Step 3: BufferGenerator implementieren**

Erstelle `src/core/BufferGenerator.ts`:
```typescript
export class BufferGenerator {
  private view: DataView;
  private uint8: Uint8Array;

  constructor(byteLength: number) {
    const buffer = new ArrayBuffer(byteLength);
    this.view = new DataView(buffer);
    this.uint8 = new Uint8Array(buffer);
  }

  private assertBounds(offset: number, size: number): void {
    if (offset + size > this.view.byteLength) {
      throw new RangeError(
        `BufferGenerator: out of bounds write at offset ${offset} (size ${size}, buffer ${this.view.byteLength})`
      );
    }
  }

  writeUint8(offset: number, value: number): void {
    this.assertBounds(offset, 1);
    this.view.setUint8(offset, value);
  }

  writeUint16LE(offset: number, value: number): void {
    this.assertBounds(offset, 2);
    this.view.setUint16(offset, value, true);
  }

  writeUint32LE(offset: number, value: number): void {
    this.assertBounds(offset, 4);
    this.view.setUint32(offset, value, true);
  }

  writeAscii(offset: number, value: string, fieldLength: number): void {
    this.assertBounds(offset, fieldLength);
    for (let i = 0; i < fieldLength; i++) {
      this.view.setUint8(offset + i, i < value.length ? value.charCodeAt(i) : 0);
    }
  }

  toUint8Array(): Uint8Array {
    return new Uint8Array(this.uint8);
  }

  toArrayBuffer(): ArrayBuffer {
    return this.uint8.buffer.slice(0);
  }
}
```

- [ ] **Step 4: Tests ausführen — müssen PASS sein**

```bash
npx vitest run tests/unit/BufferGenerator.test.ts
```
Erwartet: 5 Tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/core/BufferGenerator.ts tests/unit/BufferGenerator.test.ts
git commit -m "feat(core): add BufferGenerator with bounds checking"
```

---

### Task 11: PRSTDecoder

**Files:**
- Create: `src/core/PRSTDecoder.ts`
- Test: `tests/unit/PRSTDecoder.test.ts`

> ⚠️ **Byte-Offsets:** Die tatsächlichen Offsets im GP-200 `.prst`-Format sind noch unbekannt. Die unten stehenden Offsets sind **Platzhalter** und MÜSSEN durch Reverse Engineering realer Dateien validiert werden.

- [ ] **Step 1: Failing-Tests mit Mock-Buffer schreiben**

Erstelle `tests/unit/PRSTDecoder.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { PRSTDecoder, PRST_MAGIC } from '@/core/PRSTDecoder';

/** Erstellt einen minimalen Test-Buffer mit bekanntem Inhalt */
function buildTestBuffer(): Uint8Array {
  const buf = new Uint8Array(512).fill(0);
  // Magic-Header "PRST" (Platzhalter — echte Bytes TBD)
  buf[0] = 0x50; buf[1] = 0x52; buf[2] = 0x53; buf[3] = 0x54; // "PRST"
  // Version (Platzhalter Offset 4)
  buf[4] = 0x01;
  // Patch-Name bei Offset 8, max 12 Bytes (Platzhalter)
  'TestPatch'.split('').forEach((c, i) => { buf[8 + i] = c.charCodeAt(0); });
  return buf;
}

describe('PRSTDecoder', () => {
  it('erkennt den Magic-Header', () => {
    const buf = buildTestBuffer();
    const decoder = new PRSTDecoder(buf);
    expect(decoder.hasMagic()).toBe(true);
  });

  it('hasMagic() gibt false zurück bei leerem Buffer', () => {
    const empty = new Uint8Array(512).fill(0);
    const decoder = new PRSTDecoder(empty);
    expect(decoder.hasMagic()).toBe(false);
  });

  it('liest den Patch-Namen', () => {
    const buf = buildTestBuffer();
    const decoder = new PRSTDecoder(buf);
    expect(decoder.decode().patchName).toBe('TestPatch');
  });

  it('wirft bei ungültigem Magic', () => {
    const bad = new Uint8Array(512).fill(0);
    const decoder = new PRSTDecoder(bad);
    expect(() => decoder.decode()).toThrow('Invalid .prst file');
  });
});
```

- [ ] **Step 2: Test ausführen — muss FAIL sein**

```bash
npx vitest run tests/unit/PRSTDecoder.test.ts
```

- [ ] **Step 3: PRSTDecoder implementieren**

Erstelle `src/core/PRSTDecoder.ts`:
```typescript
import { BinaryParser } from './BinaryParser';
import { GP200PresetSchema, type GP200Preset } from './types';

// TODO: Tatsächliche Byte-Offsets durch Reverse Engineering bestätigen
export const PRST_MAGIC = 'PRST';
const OFFSET_MAGIC = 0;
const OFFSET_VERSION = 4;
const OFFSET_PATCH_NAME = 8;
const PATCH_NAME_LENGTH = 12;
// const OFFSET_EFFECTS = 20; // TODO: ermitteln
// const OFFSET_CHECKSUM = ???;  // TODO: ermitteln

export class PRSTDecoder {
  private parser: BinaryParser;

  constructor(buffer: Uint8Array) {
    this.parser = new BinaryParser(buffer);
  }

  hasMagic(): boolean {
    return this.parser.readAscii(OFFSET_MAGIC, 4) === PRST_MAGIC;
  }

  decode(): GP200Preset {
    if (!this.hasMagic()) {
      throw new Error('Invalid .prst file: magic header not found');
    }

    const version = String(this.parser.readUint8(OFFSET_VERSION));
    const patchName = this.parser.readAscii(OFFSET_PATCH_NAME, PATCH_NAME_LENGTH);

    // TODO: Effekt-Slots dekodieren sobald Format bekannt ist
    const effects = [];

    // TODO: Checksum-Offset ermitteln
    const checksum = 0;

    const preset = { version, patchName, effects, checksum };
    return GP200PresetSchema.parse(preset);
  }
}
```

- [ ] **Step 4: Tests ausführen — müssen PASS sein**

```bash
npx vitest run tests/unit/PRSTDecoder.test.ts
```
Erwartet: 4 Tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/core/PRSTDecoder.ts tests/unit/PRSTDecoder.test.ts
git commit -m "feat(core): add PRSTDecoder scaffold (byte offsets TBD)"
```

---

### Task 12: PRSTEncoder

**Files:**
- Create: `src/core/PRSTEncoder.ts`
- Test: `tests/unit/PRSTEncoder.test.ts`

- [ ] **Step 1: Failing-Tests schreiben**

Erstelle `tests/unit/PRSTEncoder.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { PRSTEncoder } from '@/core/PRSTEncoder';
import { PRSTDecoder, PRST_MAGIC } from '@/core/PRSTDecoder';
import type { GP200Preset } from '@/core/types';

const samplePreset: GP200Preset = {
  version: '1',
  patchName: 'MyPatch',
  effects: [],
  checksum: 0,
};

describe('PRSTEncoder', () => {
  it('schreibt den Magic-Header', () => {
    const encoder = new PRSTEncoder();
    const buf = encoder.encode(samplePreset);
    const arr = new Uint8Array(buf);
    const magic = String.fromCharCode(arr[0], arr[1], arr[2], arr[3]);
    expect(magic).toBe(PRST_MAGIC);
  });

  it('encode → decode ergibt das gleiche Preset (round-trip)', () => {
    const encoder = new PRSTEncoder();
    const buf = new Uint8Array(encoder.encode(samplePreset));
    const decoder = new PRSTDecoder(buf);
    const decoded = decoder.decode();
    expect(decoded.patchName).toBe(samplePreset.patchName);
    expect(decoded.version).toBe(samplePreset.version);
    expect(decoded.checksum).toBe(samplePreset.checksum);
    expect(decoded.effects).toHaveLength(samplePreset.effects.length);
  });
});
```

- [ ] **Step 2: Test ausführen — muss FAIL sein**

```bash
npx vitest run tests/unit/PRSTEncoder.test.ts
```

- [ ] **Step 3: PRSTEncoder implementieren**

Erstelle `src/core/PRSTEncoder.ts`:
```typescript
import { BufferGenerator } from './BufferGenerator';
import { GP200PresetSchema, type GP200Preset } from './types';

// TODO: Gleiche Offsets wie im Decoder — sobald Format bekannt ist anpassen
const PRST_MAGIC = 'PRST';
const TOTAL_SIZE = 512; // TODO: tatsächliche Größe ermitteln
const OFFSET_MAGIC = 0;
const OFFSET_VERSION = 4;
const OFFSET_PATCH_NAME = 8;
const PATCH_NAME_LENGTH = 12;

export class PRSTEncoder {
  encode(preset: GP200Preset): ArrayBuffer {
    GP200PresetSchema.parse(preset); // Validierung vor dem Schreiben

    const gen = new BufferGenerator(TOTAL_SIZE);

    gen.writeAscii(OFFSET_MAGIC, PRST_MAGIC, 4);
    gen.writeUint8(OFFSET_VERSION, parseInt(preset.version, 10));
    gen.writeAscii(OFFSET_PATCH_NAME, preset.patchName, PATCH_NAME_LENGTH);

    // TODO: Effekt-Slots schreiben sobald Format bekannt ist
    // TODO: Checksum berechnen und schreiben

    return gen.toArrayBuffer();
  }
}
```

- [ ] **Step 4: Tests ausführen — müssen PASS sein**

```bash
npx vitest run tests/unit/PRSTEncoder.test.ts
```
Erwartet: 2 Tests PASS

- [ ] **Step 5: Alle Unit-Tests ausführen**

```bash
npx vitest run
```
Erwartet: Alle Tests PASS

- [ ] **Step 6: Commit**

```bash
git add src/core/PRSTEncoder.ts tests/unit/PRSTEncoder.test.ts
git commit -m "feat(core): add PRSTEncoder with round-trip test"
```

---

## Chunk 3: Visual Editor

### Task 13: FileUpload-Komponente

**Files:**
- Create: `src/components/FileUpload.tsx`

- [ ] **Step 1: FileUpload schreiben**

Erstelle `src/components/FileUpload.tsx`:
```typescript
'use client';
import { useRef } from 'react';
import { useTranslations } from 'next-intl';

interface FileUploadProps {
  onFile: (buffer: Uint8Array, filename: string) => void;
}

export function FileUpload({ onFile }: FileUploadProps) {
  const t = useTranslations('home');
  const inputRef = useRef<HTMLInputElement>(null);

  function handleFile(file: File) {
    const reader = new FileReader();
    reader.onload = (e) => {
      const buffer = new Uint8Array(e.target!.result as ArrayBuffer);
      onFile(buffer, file.name);
    };
    reader.readAsArrayBuffer(file);
  }

  function handleChange(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (file) handleFile(file);
  }

  function handleDrop(e: React.DragEvent) {
    e.preventDefault();
    const file = e.dataTransfer.files[0];
    if (file) handleFile(file);
  }

  return (
    <div
      onDrop={handleDrop}
      onDragOver={(e) => e.preventDefault()}
      className="border-2 border-dashed border-gray-400 rounded-xl p-12 text-center cursor-pointer hover:border-blue-500 transition"
      onClick={() => inputRef.current?.click()}
      role="button"
      tabIndex={0}
      aria-label={t('uploadCta')}
      data-testid="file-upload-zone"
      onKeyDown={(e) => (e.key === 'Enter' || e.key === ' ') && inputRef.current?.click()}
    >
      <p className="text-lg">{t('uploadCta')}</p>
      <p className="text-sm text-gray-500 mt-2">.prst</p>
      <input
        ref={inputRef}
        type="file"
        accept=".prst"
        className="hidden"
        onChange={handleChange}
        data-testid="file-input"
        aria-hidden="true"
      />
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/FileUpload.tsx
git commit -m "feat(ui): add FileUpload component with drag-and-drop"
```

---

### Task 14: usePreset Hook

**Files:**
- Create: `src/hooks/usePreset.ts`
- Test: `tests/unit/usePreset.test.ts`

- [ ] **Step 1: Failing-Tests schreiben**

Erstelle `tests/unit/usePreset.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { usePreset } from '@/hooks/usePreset';

const samplePreset = {
  version: '1',
  patchName: 'Alt',
  effects: [],
  checksum: 0,
};

describe('usePreset', () => {
  it('initial state ist null', () => {
    const { result } = renderHook(() => usePreset());
    expect(result.current.preset).toBeNull();
  });

  it('setPatchName aktualisiert den Patch-Namen', () => {
    const { result } = renderHook(() => usePreset());
    act(() => { result.current.loadPreset(samplePreset); });
    act(() => { result.current.setPatchName('Neu'); });
    expect(result.current.preset?.patchName).toBe('Neu');
  });

  it('toggleEffect wechselt den enabled-Status eines Slots', () => {
    const presetWithSlot = {
      ...samplePreset,
      effects: [{ slotIndex: 0, effectId: 1, enabled: false, params: [] }],
    };
    const { result } = renderHook(() => usePreset());
    act(() => { result.current.loadPreset(presetWithSlot); });
    act(() => { result.current.toggleEffect(0); });
    expect(result.current.preset?.effects[0].enabled).toBe(true);
    act(() => { result.current.toggleEffect(0); });
    expect(result.current.preset?.effects[0].enabled).toBe(false);
  });

  it('reset setzt das Preset auf null', () => {
    const { result } = renderHook(() => usePreset());
    act(() => { result.current.loadPreset(samplePreset); });
    act(() => { result.current.reset(); });
    expect(result.current.preset).toBeNull();
  });
});
```

- [ ] **Step 2: Test ausführen — muss FAIL sein**

```bash
npx vitest run tests/unit/usePreset.test.ts
```

- [ ] **Step 3: usePreset implementieren**

Erstelle `src/hooks/usePreset.ts`:
```typescript
import { useState, useCallback } from 'react';
import type { GP200Preset } from '@/core/types';

interface PresetActions {
  preset: GP200Preset | null;
  loadPreset: (preset: GP200Preset) => void;
  setPatchName: (name: string) => void;
  toggleEffect: (slotIndex: number) => void;
  reset: () => void;
}

export function usePreset(): PresetActions {
  const [preset, setPreset] = useState<GP200Preset | null>(null);

  const loadPreset = useCallback((p: GP200Preset) => {
    setPreset(p);
  }, []);

  const setPatchName = useCallback((name: string) => {
    setPreset((prev) => prev ? { ...prev, patchName: name } : null);
  }, []);

  const toggleEffect = useCallback((slotIndex: number) => {
    setPreset((prev) => {
      if (!prev) return null;
      return {
        ...prev,
        effects: prev.effects.map((slot) =>
          slot.slotIndex === slotIndex ? { ...slot, enabled: !slot.enabled } : slot
        ),
      };
    });
  }, []);

  const reset = useCallback(() => {
    setPreset(null);
  }, []);

  return { preset, loadPreset, setPatchName, toggleEffect, reset };
}
```

- [ ] **Step 4: Tests ausführen — müssen PASS sein**

```bash
npx vitest run tests/unit/usePreset.test.ts
```
Erwartet: 4 Tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/hooks/usePreset.ts tests/unit/usePreset.test.ts
git commit -m "feat(hooks): add usePreset state management hook"
```

---

### Task 15: EffectSlot-Komponente

**Files:**
- Create: `src/components/EffectSlot.tsx`

- [ ] **Step 1: EffectSlot schreiben**

Ergänze zuerst i18n-Keys in den Message-Dateien:

`messages/de.json` unter `"editor"`:
```json
"effectOn": "AN",
"effectOff": "AUS",
"effectEnabled": "aktiviert",
"effectDisabled": "deaktiviert",
"effectSlotLabel": "Slot"
```

`messages/en.json` unter `"editor"`:
```json
"effectOn": "ON",
"effectOff": "OFF",
"effectEnabled": "enabled",
"effectDisabled": "disabled",
"effectSlotLabel": "Slot"
```

Erstelle `src/components/EffectSlot.tsx`:
```typescript
'use client';
import { useTranslations } from 'next-intl';
import type { EffectSlot as EffectSlotType } from '@/core/types';

interface EffectSlotProps {
  slot: EffectSlotType;
  onToggle: (index: number) => void;
}

export function EffectSlot({ slot, onToggle }: EffectSlotProps) {
  const t = useTranslations('editor');
  return (
    <div
      className={`rounded-lg border p-4 ${slot.enabled ? 'border-blue-500 bg-blue-50' : 'border-gray-300 bg-gray-50'}`}
      data-testid={`effect-slot-${slot.slotIndex}`}
    >
      <div className="flex items-center justify-between">
        <span className="font-medium">{t('effectSlotLabel')} {slot.slotIndex + 1}</span>
        <button
          onClick={() => onToggle(slot.slotIndex)}
          aria-pressed={slot.enabled}
          aria-label={`${t('effectSlotLabel')} ${slot.slotIndex + 1} ${slot.enabled ? t('effectEnabled') : t('effectDisabled')}`}
          data-testid={`effect-slot-toggle-${slot.slotIndex}`}
          className={`px-3 py-1 rounded text-sm transition ${
            slot.enabled
              ? 'bg-blue-600 text-white hover:bg-blue-700'
              : 'bg-gray-300 text-gray-700 hover:bg-gray-400'
          }`}
        >
          {slot.enabled ? t('effectOn') : t('effectOff')}
        </button>
      </div>
      <div className="mt-2 text-sm text-gray-500">
        Effect ID: {slot.effectId} · Parameter: {slot.params.length}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/EffectSlot.tsx
git commit -m "feat(ui): add EffectSlot component with toggle"
```

---

### Task 16: Editor-Seite zusammenbauen

**Files:**
- Modify: `src/app/[locale]/editor/page.tsx`
- Note: `messages/de.json` und `messages/en.json` wurden bereits in Task 15 mit allen Editor-Keys ergänzt.

- [ ] **Step 1: Editor-Seite implementieren**

Erstelle `src/app/[locale]/editor/page.tsx`:
```typescript
'use client';
import { useTranslations } from 'next-intl';
import { FileUpload } from '@/components/FileUpload';
import { EffectSlot } from '@/components/EffectSlot';
import { usePreset } from '@/hooks/usePreset';
import { PRSTDecoder } from '@/core/PRSTDecoder';
import { PRSTEncoder } from '@/core/PRSTEncoder';
import { useCallback } from 'react';

export default function EditorPage() {
  const t = useTranslations('editor');
  const { preset, loadPreset, setPatchName, toggleEffect } = usePreset();

  const handleFile = useCallback((buffer: Uint8Array) => {
    try {
      const decoder = new PRSTDecoder(buffer);
      loadPreset(decoder.decode());
    } catch (err) {
      alert(`Fehler beim Laden: ${err instanceof Error ? err.message : String(err)}`);
    }
  }, [loadPreset]);

  function handleDownload() {
    if (!preset) return;
    const encoder = new PRSTEncoder();
    const ab = encoder.encode(preset);
    const blob = new Blob([ab], { type: 'application/octet-stream' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `${preset.patchName}.prst`;
    a.click();
    URL.revokeObjectURL(url);
  }

  if (!preset) {
    return (
      <div className="p-8 max-w-2xl mx-auto">
        <h1 className="text-2xl font-bold mb-6">{t('title')}</h1>
        <FileUpload onFile={handleFile} />
      </div>
    );
  }

  return (
    <div className="p-8 max-w-2xl mx-auto">
      <h1 className="text-2xl font-bold mb-6">{t('title')}</h1>
      <div className="mb-6">
        <label htmlFor="patch-name" className="block text-sm font-medium mb-1">
          {t('patchName')}
        </label>
        <input
          id="patch-name"
          type="text"
          value={preset.patchName}
          onChange={(e) => setPatchName(e.target.value)}
          maxLength={12}
          data-testid="patch-name-input"
          className="border rounded px-3 py-2 w-full max-w-xs"
        />
      </div>
      <div className="grid gap-3 mb-6">
        {preset.effects.map((slot) => (
          <EffectSlot
            key={slot.slotIndex}
            slot={slot}
            onToggle={toggleEffect}
          />
        ))}
      </div>
      <button
        onClick={handleDownload}
        data-testid="download-btn"
        className="px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 transition"
      >
        {t('download')}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Build prüfen**

```bash
npm run build
```
Erwartet: Build ohne Fehler.

- [ ] **Step 3: Commit**

```bash
git add src/app/[locale]/editor/page.tsx src/components/EffectSlot.tsx messages/
git commit -m "feat(ui): wire up editor page with upload, edit, and download"
```

---

### Task 17: E2E Test-Suite

**Files:**
- Create: `tests/e2e/editor.spec.ts`
- Create: `tests/e2e/a11y.spec.ts`

- [ ] **Step 1: Haupt-E2E-Test schreiben**

Erstelle `tests/e2e/editor.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Home Page', () => {
  test('zeigt Headline und Upload-CTA', async ({ page }) => {
    await page.goto('/de');
    await expect(page.getByTestId('home-upload-cta')).toBeVisible();
    await expect(page.locator('h1')).toContainText('GP-200');
  });

  test('Navbar zeigt Sprachschalter', async ({ page }) => {
    await page.goto('/de');
    await expect(page.getByTestId('nav-locale-switcher')).toBeVisible();
  });

  test('Sprache wechseln zu EN', async ({ page }) => {
    await page.goto('/de');
    await page.getByTestId('nav-locale-switcher').click();
    await expect(page).toHaveURL(/\/en/);
    await expect(page.locator('h1')).toContainText('GP-200');
  });
});

test.describe('Editor Page', () => {
  test('zeigt Upload-Zone wenn kein Preset geladen', async ({ page }) => {
    await page.goto('/de/editor');
    await expect(page.getByTestId('file-upload-zone')).toBeVisible();
  });

  test('zeigt Editor-UI nach Preset-Upload', async ({ page }) => {
    await page.goto('/de/editor');

    // Minimalen Test-Buffer erstellen: "PRST" Magic + Patch-Name "TestPatch"
    const testPrest = Buffer.alloc(512, 0);
    testPrest.write('PRST', 0, 'ascii');  // Magic Header
    testPrest[4] = 0x01;                  // Version
    testPrest.write('TestPatch', 8, 'ascii'); // Patch Name

    // Datei über den versteckten File-Input hochladen
    const fileInput = page.locator('[data-testid="file-input"]');
    await fileInput.setInputFiles({
      name: 'test.prst',
      mimeType: 'application/octet-stream',
      buffer: testPrest,
    });

    // Editor-UI muss erscheinen
    await expect(page.getByTestId('patch-name-input')).toBeVisible();
    await expect(page.getByTestId('download-btn')).toBeVisible();

    // Patch-Name aus Datei muss im Input stehen
    await expect(page.getByTestId('patch-name-input')).toHaveValue('TestPatch');
  });
});
```

- [ ] **Step 2: A11y-Test schreiben**

Erstelle `tests/e2e/a11y.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

test.describe('A11y: Home Page', () => {
  test('hat keine kritischen Accessibility-Verstöße', async ({ page }) => {
    await page.goto('/de');
    const results = await new AxeBuilder({ page })
      .withTags(['wcag2a', 'wcag2aa'])
      .analyze();
    expect(results.violations).toHaveLength(0);
  });
});

test.describe('A11y: Editor Page', () => {
  test('hat keine kritischen Accessibility-Verstöße', async ({ page }) => {
    await page.goto('/de/editor');
    const results = await new AxeBuilder({ page })
      .withTags(['wcag2a', 'wcag2aa'])
      .analyze();
    expect(results.violations).toHaveLength(0);
  });
});
```

- [ ] **Step 3: Dev-Server starten und E2E-Tests ausführen**

```bash
npm run build && npm run start &
npx playwright test
```
Erwartet: Alle E2E-Tests PASS (oder bekannte A11y-Findings dokumentieren)

- [ ] **Step 4: Alle Tests (Unit + E2E) ausführen**

```bash
npx vitest run && npx playwright test
```

- [ ] **Step 5: Final Commit**

```bash
git add tests/e2e/
git commit -m "test(e2e): add Playwright tests with axe-core A11y checks"
```

---

## Nächste Schritte (nach Abschluss)

1. **Binary-Format reverse-engineeren:** Echte `.prst`-Datei vom GP-200 laden und mit einem Hex-Editor analysieren. Offsets in `PRSTDecoder.ts` und `PRSTEncoder.ts` ausfüllen.
2. **Effekt-Datenbank:** Eine JSON-Datei mit allen Effekt-IDs und Parameter-Definitionen anlegen.
3. **EffectSlot parametrisch:** Parameter-Slider/Inputs für jeden Effekttyp.
4. **CI/CD:** GitHub Actions für `vitest` + `playwright` einrichten.
